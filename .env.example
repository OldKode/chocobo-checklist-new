# Pooled runtime connection string from Neon (host includes -pooler)
DATABASE_URL="postgresql://user:password@project-pooler.region.aws.neon.tech/chocobo?sslmode=require"

# Direct connection string from Neon for Prisma CLI / migrations
DIRECT_URL="postgresql://user:password@project.region.aws.neon.tech/chocobo?sslmode=require"

# Public app URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"
